#include <stdio.h>

int main(int argc, char const *argv[])
{
    // 6. Assumindo que pulo[] é um vetor do tipo int, quais das seguintes expressões referenciam o valor do terceiro elemento do vetor?

    /*
        *(pulo + 2);
        *(pulo + 4);
        pulo + 4;
        pulo + 2;

        a expressão *(pulo) referencia o valor do primeiro elemento, ja a expressão *(pulo + 2); anda dois endereços para frente e referencia o valor do terceiro elemento
    */
    return 0;
}

#include <stdio.h>

int main(int argc, char const *argv[])
{
    // 8.O que fazem os seguintes programas em C?

    /*
        int main(){
            int vet[] = {4,9,13};
            int i;
            for(i=0;i<3;i++){
                printf("%d ",*(vet+i));
            }
        }
        Primeiro cria um array com 3 elementos e depois
        percorre, dentro de um laço, e imprimime os valores
        dos elementos do array
    */

    /*
        int main(){
            int vet[] = {4,9,13};
            int i;
            for(i=0;i<3;i++){
                printf("%X ",vet+i);
            }
        }

        Primeiro cria um array com 3 elementos e depois
        percorre, dentro de um laço, e imprimime os endereços
        dos elementos do array
    */
   
    return 0;
}
